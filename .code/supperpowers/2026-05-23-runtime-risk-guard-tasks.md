# 运行期风控阈值与幂等保护任务清单

## 目标
- 增加最小可用的运行期风控保护，降低短时间重复执行、重复下单和低价值噪音执行风险

## 任务列表
- [x] 盘点当前主任务串行化、未成交订单检测与订单追踪逻辑
- [x] 设计并实现最小风控批次
- [x] 补充测试覆盖冷却时间、最小可下单总额与幂等保护
- [x] 同步 README、示例配置与路线图
- [x] 跑全量测试与 race 验证

## 当前实现范围
- 本批次只实现以下三项：
  - 主策略执行冷却时间
  - 最小可下单总额阈值
  - 基于订单指纹的短窗口幂等保护
- 暂不引入复杂风控规则引擎
- 暂不改 Telegram 指令集合，先通过配置接入

## 当前进展补充
- 已补充人工触发冷却豁免：Telegram `/run`、`/restart` 这类人工触发默认绕过执行冷却，但仍保留最小执行资金阈值和订单指纹幂等保护。
- 已补充最近一次策略决策摘要的结构化跳过原因：当前会记录并展示触发来源、是否冷却豁免、跳过原因，便于通过日志和 `/decisionsummary` 快速定位“为什么本轮没有下单”。
- 已完成验证：
  - `go test ./internal/strategy -count=1`
  - `go test ./internal/telegram -count=1`
  - `go test ./...`
  - `go test -race ./...`
