# 借贷订单通知功能说明

## 📋 功能概述

借贷检查功能会按 `LENDING_CHECK_MINUTES` 定时轮询 Bitfinex 的活跃 `FundingCredits` 与 Funding 钱包余额，并通过 Telegram 发送变化通知。

## ⚙️ 配置参数

### `LENDING_CHECK_MINUTES`
- **说明**: 借贷订单检查间隔（分钟）
- **默认值**: 10 分钟
- **建议值**: 5-30 分钟
- **示例**: 
  ```yaml
  LENDING_CHECK_MINUTES: 10  # 每10分钟检查一次新的借贷订单
  ```

### `NOTIFICATION_FORMAT`
- **说明**: 借贷通知与 `/offers` 输出格式
- **可选值**:
  - `classic`: 现有纯文本格式
  - `aligned`: 使用 `<pre>` 对齐块显示，且 `usd` 金额会显示为 `$434.62`
- **默认值**: `classic`
- **示例**:
  ```yaml
  NOTIFICATION_FORMAT: "aligned"
  ```

## 🔄 调度器架构

应用程序现在有三个独立的调度器：

1. **主要任务调度器** (`MINUTES_RUN`)
   - 执行下单、取消等交易操作
   - 默认间隔由 `MINUTES_RUN` 控制

2. **借贷检查调度器** (`LENDING_CHECK_MINUTES`)
   - 检查新的借贷成交
   - 检查贷出已结束/返还
   - 检查可用余额显著变化
   - 检查借贷查询是否连续失败
   - 独立于主要任务运行

3. **利率检查调度器** (固定每小时)
   - 检查利率是否超过阈值
   - 发送利率提醒

## 📬 变化通知对照表

`LENDING_CHECK_MINUTES` 这条检查链路目前会触发以下 4 类变化通知：

| 通知标题 | 触发条件 | 说明 |
| --- | --- | --- |
| `💰 新的借贷订单通知` | 当前活跃 `FundingCredits` 中出现了上一轮没有的新 credit | 表示检测到新的贷出成交 |
| `💸 贷出已结束/返还通知` | 上一轮活跃的 credit，本轮已不在活跃 `FundingCredits` 列表中 | 表示贷出已结束或已返还 |
| `💰 可用余额变化通知` | 可用余额显著增加，且本轮没有识别到新的 credit | 用作成交检测的兜底提醒 |
| `⚠️ 借贷检查连续失败` | 查询 Funding 钱包或活跃 `FundingCredits` 失败 | 用于提示 API / nonce / 多实例共用 key 等异常 |

### 1. 新的借贷订单通知

示例：

```text
💰 新的借贷订单通知

📊 订单 #1
💵 金额: 1000.00 USD
📈 日利率: 0.1200%
⏰ 期间: 30 天
💰 预期收益: 36.0000 USD
🕐 开始时间: 2025-06-29 14:30:15

📊 统计信息:
📦 总数量: 1 个订单
💵 总金额: 1000.00 USD
💰 总预期收益: 36.0000 USD
```

若 `NOTIFICATION_FORMAT: "aligned"`，则会使用对齐块格式，类似：

```text
💰 新的借贷订单通知

📊 订单 #1 (ID: 123456789)
金额　　：$434.62
日利率　：0.0390%
期间　　：120 天
预期收益：$20.34
开始时间：2026-05-22 13:30:00

📊 统计信息
总订单数　：1
总金额　　：$434.62
总预期收益：$20.34
```

### 2. 贷出已结束/返还通知

示例：

```text
💸 贷出已结束/返还通知

📊 订单 #1 (ID: 123456789)
💵 金额: 1000.00 USD
📈 日利率: 0.1200%
⏰ 期间: 30 天
🕐 开始时间: 2025-06-29 14:30:15
🧾 状态: ACTIVE
🔔 检测时间: 2025-06-30 14:35:00

📊 统计信息:
📦 总数量: 1 个订单
💵 总金额: 1000.00 USD
```

说明：
- 当前实现是根据“上一轮活跃、本轮消失”来判断已结束/返还
- 文案刻意使用“已结束/返还”，避免把活跃列表消失过度解释成单一原因
- `aligned` 格式下同样会使用对齐块

### 3. 可用余额变化通知

触发条件：
- 本轮可用余额相较上一轮显著增加
- 且本轮没有识别到新的 `FundingCredits`

目前显著增加阈值为：

```text
max(上一轮余额的 10%, MIN_LOAN)
```

这类通知主要作为成交识别的兜底提醒。

### 4. 借贷检查连续失败

示例：

```text
⚠️ 借贷检查连续失败

原因: [API_CALL] failed to get wallets: ... nonce: small (10114)

机器人暂时无法确认新的贷出成交或余额变化，请检查 Bitfinex API key、nonce 状态与是否存在多实例共用同一组 key。
```

说明：
- 同一波相同错误只通知一次，避免刷屏
- 检查恢复成功后，若后续再次失败，会重新通知

## 🎛️ Telegram 指令

### `/lending`
查看当前所有活跃的借贷订单，包含：
- 订单详细信息
- 收益计算
- 年化收益率统计

### `/offers`
查看当前未成交订单。

- `classic` 格式下使用原有纯文本输出
- `aligned` 格式下使用对齐块输出，并对 `usd` 使用 `$` 金额格式

## 🔧 工作原理

1. **时间戳记录**：记录上次检查时间 (`LastLendingCheckTime`)
2. **新订单检测**：比较当前活跃 credit 与上一轮已见 ID 集合
3. **结束/返还检测**：比较上一轮活跃 credit 快照与当前活跃 credit 快照
4. **余额兜底检测**：比较当前可用余额与上一轮可用余额
5. **首次启动处理**：启动后的首次检查只初始化快照，不补发历史通知
6. **错误处理**：Telegram 未认证或发送失败时记录到日志，不中断主程序

## 📊 配置建议

### 测试环境
```yaml
MINUTES_RUN: 5              # 主要任务：5分钟
LENDING_CHECK_MINUTES: 5    # 借贷检查：5分钟
TEST_MODE: true            # 启用测试模式
```

### 正式环境
```yaml
MINUTES_RUN: 15             # 主要任务：15分钟
LENDING_CHECK_MINUTES: 10   # 借贷检查：10分钟
TEST_MODE: false           # 正式模式
```

### 高频监控
```yaml
MINUTES_RUN: 10             # 主要任务：10分钟
LENDING_CHECK_MINUTES: 3    # 借贷检查：3分钟（更频繁通知）
```

## 📝 日志输出

启动时会显示调度器配置：
```
⚙️ 主要任务间隔: 15 分钟
💰 借贷检查间隔: 10 分钟
📊 利率检查: 每小时
```

检查时的日志：
```
检查执行触发条件（新借贷订单、余额变化）...
首次检查，发现 5 个现有借贷订单，余额: 1000.00，初始化检查参数
```

通知时的日志：
```
发现 2 个新的借贷订单
借贷订单通知发送成功
贷出已结束/返还通知发送成功
余额变化通知发送成功
```

## ⚠️ 注意事项

1. **首次启动**：不会对当前已存在的活跃借贷订单补发历史通知
2. **无持久化快照**：程序重启期间发生的成交或返还事件不会补发
3. **Telegram 认证**：未认证时通知失败会记录到日志
4. **API 限制**：检查间隔不建议小于 3 分钟
5. **测试模式**：不影响借贷检查功能，仍会检查真实订单
6. **返还判定语义**：当前是根据活跃 `FundingCredits` 消失来判断“已结束/返还”，不是直接读取独立的返还事件流

## 🚀 升级说明

从旧版本升级时，新增的配置参数会自动设置默认值：
- 如未设置 `LENDING_CHECK_MINUTES`，默认为 10 分钟
- 现有的 `MINUTES_RUN` 配置不受影响
- 借贷检查功能会自动启用
